const { Command } = require("stackerjs");

export class __COMMAND_NAME__ extends Command 
{
    constructor() 
    {
        super(...arguments);

        this.name = "__COMMAND_NAME__";
        this.description = "";
        this.route = "";
    }

    handle() 
    {
        // Write your code here
    }
}
